/*
 * This Java source file was generated by the Gradle 'init' task.
 */
package cicd.gradle.app;

import cicd.gradle.list.LinkedList;

import static cicd.gradle.utilities.StringUtils.join;
import static cicd.gradle.utilities.StringUtils.split;
import static cicd.gradle.app.MessageUtils.getMessage;

import org.apache.commons.text.WordUtils;

public class App {
    public static void main(String[] args) {
        LinkedList tokens;
        tokens = split(getMessage());
        String result = join(tokens);
        System.out.println(WordUtils.capitalize(result));
    }
}
